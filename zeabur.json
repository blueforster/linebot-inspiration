{
  "app_type": "python",
  "build_command": "pip install -r requirements.txt",
  "start_command": "gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60",
  "install_command": "pip install --upgrade pip"
}